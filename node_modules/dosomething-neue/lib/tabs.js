"use strict";

var _interopRequire = function (obj) { return obj && obj.__esModule ? obj["default"] : obj; };

/**
 * Initialize and attach event handlers for Tabs pattern.
 */

var $ = _interopRequire(require("jquery"));

$(document).ready(function () {

  var $tabs = $(".js-tabs");
  var $tabMenuLinks = $tabs.find(".tabs__menu a");

  // Show the first tab in any "js-tabs" collection.
  $tabs.each(function () {
    $(this).find(".tabs__tab").first().addClass("is-active");
  });

  // View other tabs on click.
  $tabMenuLinks.on("click", function (event) {
    event.preventDefault();

    var $siblings = $(this).parent().siblings();
    var selection = $(this).data("tab") - 1;
    var $tabs = $(this).closest(".js-tabs").find(".tabs__tab");
    var tab = $tabs.get(selection);

    $siblings.removeClass("is-active");
    $(this).parent().addClass("is-active");

    // Show selected tab.
    $tabs.removeClass("is-active");
    $(tab).addClass("is-active");
  });
});